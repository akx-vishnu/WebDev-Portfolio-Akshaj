// Hero3D removed — SceneBackground already renders a full particle tunnel background.
// Having a second Canvas here was doubling GPU load with zero visual gain.
// The SceneBackground (fixed, z-[-1]) shows through and provides the star/particle effect.

const Hero3D = () => null;

export default Hero3D;
